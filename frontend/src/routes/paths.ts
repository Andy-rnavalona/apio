/**
 * Chemins centralisés de l'application.
 * On référence toujours ces constantes plutôt que des chaînes en dur,
 * pour éviter les fautes de frappe et faciliter les renommages.
 */
export const paths = {
  home: '/',
  login: '/login',
  transaction:'/transaction'
} as const
